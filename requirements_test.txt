# Pulls in a matching `homeassistant` core (2026.9.2, live is 2026.9.1) plus recorder deps
# (SQLAlchemy etc.) transitively — nothing else needs pinning here.
pytest-homeassistant-custom-component==0.13.365
